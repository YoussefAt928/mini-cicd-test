name: Deploy to PROD

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Copy files to PROD Server
        run: |
          rsync -avz --delete ./ root@192.168.1.29:/var/www/mini-prod
